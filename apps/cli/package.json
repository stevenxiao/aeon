{
  "name": "aeon-cli",
  "version": "0.1.0",
  "license": "MIT",
  "private": true,
  "description": "Non-interactive command-line control of an Aeon repo — the dashboard's features as commands. Reuses apps/dashboard/lib so both surfaces share one source of truth.",
  "type": "module",
  "bin": {
    "aeon": "./aeon"
  },
  "scripts": {
    "typecheck": "../dashboard/node_modules/.bin/tsc --noEmit -p tsconfig.json",
    "lint": "../dashboard/node_modules/.bin/eslint src"
  },
  "dependencies": {
    "tsx": "^4.23.12",
    "yaml": "^2.9.0"
  }
}
